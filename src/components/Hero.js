const Hero = () => {
    return (
        <section className="hero" id="home">
            <h1>Build Faster with React</h1>
            <p>A modern landing page built using React.js</p>
            <button>Get Started</button>
        </section>
    );
};

export default Hero;
